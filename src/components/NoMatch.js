import React from 'react';

const NoMatch = React.createClass({
  render: function () {
    return (
      <div>
        <h1>You've strayed off the path. Maybe try going home for now?</h1>
      </div>
    )
  }
});

export default NoMatch;